import React from "react";
import "./styles.scss";

const UserInfoTitle = ({ title }) => (
  <div class="user-info">
    <h1 class="user-info__title">{title}</h1>
  </div>
);

export default UserInfoTitle;
